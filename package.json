{
  "name": "sacho-chronicles",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "사초: 춘추필법 (Sacho: The Silent Brush) - 조선시대 사관 정치 시뮬레이션 웹게임 프로토타입",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "tsx --test tests/*.test.ts",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.6.2",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "lucide": "^1.47.0"
  }
}
